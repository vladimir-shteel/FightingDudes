# План: Прототип Fortress Merge + Merge Factory

Рабочая ветка для интеграции поверх существующего прототипа: `codex/fortress-merge-from-master`.
Отдельная ветка для эксперимента "с нуля": `experiment/fortress-merge-mf-fm`.
Источник требований: `Промты MF+FM.txt` (Промт 1).

## 0. Цель и стратегия

Цель — не переписать игру с пустого листа и не просто приклеить новый экран к старому прототипу. Цель — **переделать существующий master-прототип под новое видение Fortress Merge + Merge Factory**, максимально переиспользуя уже работающие части.

Что уже есть в `master` и должно быть переиспользовано:
- экономика ресурсов, шахты, слоты рабочих и прогресс добычи;
- покупка/мердж человечков;
- подготовка юнитов и часть боевой математики/визуальных эффектов;
- общий статический стек GitHub Pages, JSON-конфиги, game loop и UI-рендер.

Что должно измениться принципиально:
- верхняя часть игры становится не старым battlefield/bridgehead, а **полем застройки 5×7 в стиле Fortress Merge**;
- кнопка «В бой» запускает бой **на поле застройки**, где враги атакуют здания, а здания/союзники защищают штаб;
- нижняя часть игры становится **экраном производства ресурсов**, основанным на текущих шахтах и мердже, но приведённым к требованиям промта;
- старые термины и UI (`Fighting Dudes`, `Garrison`, `Bridgehead`, бой против замка) постепенно заменяются на логику и подачу Fortress Merge + Merge Factory.

Иными словами: **нижнюю экономику адаптируем, верхний build-field и field-battle строим заново, старый бой используем только как источник подходов/утилит, а не как финальную механику**.

Не делаем в этой ветке:
- не удаляем существующий master-прототип ради "чистого" старта;
- не оставляем старый battlefield как основной бой;
- не создаём два независимых режима, которые выглядят как две разные игры;
- не ломаем работающую экономику до появления функциональной замены.

Идея конечного прототипа: два экрана, свайп вверх/вниз переключает между ними.
- **Верхний экран** — поле застройки 5×7 тайлов, покупка/размещение/апгрейд зданий, кнопка «В бой», волны врагов.
- **Нижний экран** — куча человечков-рабочих, мердж, 4 шахты (дерево/камень/металл/кристаллы), генерация ресурсов.

Ресурсы целевой модели: `wood`, `stone`, `iron`, `crystal`, `gold`. В текущем master-прототипе ресурс камня называется `ore`; при интеграции либо переименовываем его в `stone` миграционным шагом, либо временно используем `ore` как внутренний ключ камня с явным UI-label `Stone`.

Игровой цикл: копим ресурсы на нижнем экране → покупаем/размещаем/апгрейдим здания на верхнем → «В бой» → победа даёт золото + выбор апгрейда из 3 случайных → следующая волна. 7 волн всего.

---

## Текущая остановка

Дата отметки: 2026-07-06.
Ветка: `codex/fortress-merge-from-master`.

Каркас прототипа собран: два экрана + свайп, 5×7 build-field, магазин, экономика, fortress-бой, апгрейды, 7 волн. Автоматический QA §9.2/§9.3/§9.4/§9.5 зелёный. Дальше — приведение к промту и ручной прогон.

## Отклонения от промта, по приоритету

Ниже — актуальный список того, что реализовано не по спеке `Промты MF+FM.txt`. Отсортировано от самого заметного к самому мелкому. Ручной прогон §9.1 идёт последним, после того как отклонения устранены (или осознанно оставлены).

### P0 — заметные UX-несоответствия

- [x] **P0.1. Магазин зданий: горизонтальный зациклённый скролл.** ✅
  `#fortressShop` теперь `display:flex; overflow-x:auto`, карточки шириной 128px, `flex:0 0 128px`. Список из 8 типов рендерится в 3 копии подряд, scroll стартует на средней. При выходе за пределы средней копии (`scrollLeft < 0.5*blockWidth` или `> 2.5*blockWidth`) handler бесшовно телепортирует на эквивалент в середине.

- [x] **P0.2. Иконки Stables и Mage Tower.** ✅
  `stables.icon = "🐎"`, `mageTower.icon = "🔮"` в `data/fortress-buildings.json`. Заодно в `fetchJson` включён `cache: "no-store"`, чтобы правки контента в JSON подхватывались без танцев с cache bypass.

### P1 — крупные механики, отличающиеся от промта

- [~] **P1.1. Перемещение зданий пальцем (drag).**
  Решено оставить tap-to-move: клик по зданию (селект) → клик по свободному тайлу (переставили). Функционально эквивалент drag'а и не мешает тестировать во время итераций. Возврат к drag — только когда прототип общей играбельности пройдёт и будет запрос на «ощущение оригинала».

- [~] **P1.2. Куча человечков — свободное 2D-позиционирование.**
  Отложено до пункта «ощущение оригинала» вместе с P1.1: сейчас грид карточек, драга нет — упрощает тестирование. Возврат — после общей играбельности.

- [~] **P1.3. Перетаскивание воркеров в слоты шахт.**
  Отложено вместе с P1.1/P1.2. Tap-select остаётся основным вводом.

### P2 — баланс возвращён к промту ✅

Все P2-пункты приведены к цифрам из `Промты MF+FM.txt`. Проверка: 1 barracks на промт-числах W1 не выигрывает (defeat, ticks 2122), 2 barracks + 2 wall — победа за ~18с. То есть игрок вынужден накопить ресурсы перед волной, как и подразумевал промт.

- [x] **P2.1.** HP всех спавнеров/турелей — 50 на всех уровнях.
- [x] **P2.2.** Cooldown спавнеров: Barracks/MageTower 12/10/8s, Archery/Stables 18/15/12s, Turret 2s.
- [x] **P2.3.** Warrior hp 35 attack 3 cd 1s.
- [x] **P2.4.** Все `slotProductionMultipliers` в `mine-levels.json` = 1 (X зависит только от уровня рабочего).
- [x] **P2.5.** `passiveGoldPerSecondPerUnlockedMine = 0`.
- [x] **P2.6.** `startingGold = 20` (хватает на 3 воркера с растущей ценой 5/6/7).

**Пост-навигационный пересчёт баланса (после P3.1):**
- Enemy `speedTilesPerSecond` 0.75 → 0.55: до pathfinding враги шли по прямой и защита успевала спавнить юнитов быстрее, чем они добегают. После A* добавились обходы, но при 0.75 враги всё равно доходили до HQ раньше первого warrior. 0.55 даёт защите ~10 секунд до контакта.
- `UNIT_COLLISION_RADIUS` 0.32 → 0.18: с 0.32 минимальная дистанция между юнитами 0.64 была больше warrior range 0.5 — melee-юниты не могли ударить противника. При 0.18 (minDistance 0.36) все melee-контакты живые.
- W6: 14 врагов / 1s → 12 / 1s (был слишком плотный, чем W7); victory gold 72 → 78.
- W7: 12 врагов / 1s → 16 / 1s (босс-волна должна быть тяжелее W6); victory gold 100 → 120.

Проверено симуляцией:
- W1-W3 проходятся минимальной защитой (2 Barracks + 1 Wall).
- W4-W5 требуют апгрейдов (B3+turret).
- W6 требует максимальный комп (B3+T2+BW2 lvl3).
- W7 — стабильно при B3+T3+BW2 + archery/mines; без разнообразия результат стохастичен (2 из 3 побед).

### P3 — косметика / боевая логика

- [x] **P3.1. Навигация врагов + коллизии между юнитами.** ✅ (гибрид: A* по клеткам + мягкий repel)

  Что сделано:
    - Новый модуль `js/game/systems/pathfinding.js`: 4-neighbour A* на сетке 5×7, Manhattan-heuristic.
    - В `fortressBattleSystem.js`:
      - `getBlockedTileSetForActor` — блокирует obstacle-тайлы и футпринт живых зданий (кроме цели врага и trap mine).
      - `ensureEnemyPath` / `followPath` — враг ре-планирует маршрут каждые 0.4с или при смене цели, идёт по клеткам через `moveToward(waypoint)`, при недостижимости — fallback на прямую.
      - `resolveUnitCollisions` — pairwise-repel по радиусу 0.32, deterministic jitter при полном overlap.
      - Атака здания теперь по расстоянию до ближайшего футпринт-тайла (≤ 0.7), а не до center-of-mass — иначе враг «стоит внутри стены».
    - Старый `battlePhysics.js` не возвращали: planck-физика для сетки 5×7 избыточна, гибрид A*+repel даёт нужный эффект.

  Влияние на баланс (замечено сразу): W1 с 2 barracks + 2 wall до pathfinding'а решалась за ~18с (враги шли сквозь стены), после — за ~111с. Ожидается, что придётся заново подкрутить cd/hp зданий или скорости врагов после ручного §9.1.

## Проверка после устранения отклонений

- [ ] §9.1 — Ручной прогон w1→w7 в браузере с реальной экономикой и апгрейдами. Валидируем: экономика позволяет расти, апгрейды меняют игру, поражение реально возможно. Каждый P2-возврат к цифрам промта проверяется на этом же прогоне.

## Инвариант QA (уже зелёное)

Регрессии не терять при последующих правках:

- §9.2 — Merge на слоте шахты сохраняет слот (прогресс сбрасывается — плановое поведение).
- §9.3 — Разрушенные здания восстанавливаются на своих тайлах с полным HP.
- §9.4 — Разблокировка шахты через апгрейд сразу переключает UI.
- §9.5 — Прогон 7 волн через `tickFortressBattle` без warn/error в консоли.
- Debug-hook `window.__game = { state, ui, CONFIG }` живёт в `js/main.js` для скриптового QA.
